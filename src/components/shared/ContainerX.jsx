import React from 'react';

const ContainerX = ({children}) => {
    return (
        <div className='max-w-screen-xl mx-auto'>
            {children}
        </div>
    );
};

export default ContainerX;